// @ts-check
/** White-box testing membrane for stateful HTTP bootstrap state/environment policy. */

export {
    DEFAULT_MAX_SESSIONS,
    DEFAULT_SESSION_TTL_MS,
    DEFAULT_STATEFUL_ENV_FILE,
    buildStatefulProcessEnv,
    ensureStatefulEnvFile,
} from '../runtime.js';
