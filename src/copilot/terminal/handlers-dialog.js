// @ts-check
/**
 * src/copilot/terminal/handlers-dialog.js
 *
 * Shim de compatibilidade — re-exporta de `handlers/dialog.js`.
 *
 * @module copilot/terminal/handlers-dialog
 */

export {
    handleDeleteMemory,
    handleHubHealth,
    handleListSessions,
    handleListTurns,
    handleRecallMemories,
    handleStoreMemory,
} from './handlers/dialog.js';
