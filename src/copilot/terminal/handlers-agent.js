// @ts-check
/**
 * src/copilot/terminal/handlers-agent.js
 *
 * Shim de compatibilidade — re-exporta de `handlers/agent.js`.
 *
 * @module copilot/terminal/handlers-agent
 */

export {
    handleAcceptHandoff,
    handleDialogPause,
    handleDialogResume,
    handleGetContext,
    handleGetHandoffs,
    handleInject,
    handlePipeline,
    handleRejectHandoff,
} from './handlers/agent.js';
