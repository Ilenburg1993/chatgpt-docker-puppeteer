// @ts-check
/**
 * Filesystem invalidation public namespace.
 *
 * Operational invalidation buses are runtime-owned composition details. Runtime public access is limited to the
 * read-only startup replay child surface.
 *
 * @module copilot/infra/public/filesystem/invalidation
 */
export {};
