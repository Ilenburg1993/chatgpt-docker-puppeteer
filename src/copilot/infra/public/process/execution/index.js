// @ts-check
/** Public runtime surface for generic buffered process execution. */

export {
    DEFAULT_BUFFERED_PROCESS_MAX_BYTES,
    DEFAULT_BUFFERED_PROCESS_TIMEOUT_MS,
    MAX_BUFFERED_PROCESS_MAX_BYTES,
    MAX_BUFFERED_PROCESS_TIMEOUT_MS,
    executeBufferedProcess,
} from '../../../process/execution/index.js';
