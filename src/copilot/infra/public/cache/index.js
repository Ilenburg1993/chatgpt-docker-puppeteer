// @ts-check
/**
 * Cache public namespace.
 *
 * Runtime callers must choose an intent-specific child surface (`keys` or `tiering`). Stateful cache ownership
 * controls are diagnostic/test-only and intentionally not projected here.
 *
 * @module copilot/infra/public/cache
 */
export {};
