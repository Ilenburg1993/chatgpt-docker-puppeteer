// @ts-check
/**
 * Composition-only public namespace.
 *
 * This directory intentionally has no aggregate runtime exports. Consumers must choose one explicit composition
 * entrypoint so authority/lifecycle construction remains visible in the dependency graph.
 *
 * @module copilot/infra/public/composition
 */
