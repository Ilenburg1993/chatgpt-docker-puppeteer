// @ts-check
/**
 * Workspace composition namespace marker. Authority, IO and indexing are intentionally distinct entrypoints so a
 * consumer pays only for the capability it composes.
 * @module copilot/infra/public/composition/workspace
 */
