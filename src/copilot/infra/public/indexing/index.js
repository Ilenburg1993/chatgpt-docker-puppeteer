// @ts-check
/**
 * Indexing public namespace marker. There is intentionally no aggregate alias: consumers must choose registry,
 * file-context, search, context or another explicit capability entrypoint.
 * @module copilot/infra/public/indexing
 */
