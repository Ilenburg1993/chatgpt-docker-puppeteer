// @ts-check
/**
 * Compat layer de PermissionController para hooks.
 *
 * A implementação canônica foi promovida para `sdk/session/permission-controller.js` para reduzir acoplamento de
 * runtime com a camada `hooks/`.
 *
 * @module copilot/hooks/permission-controller
 */

export { PermissionController } from '../sdk/session/permission-controller.js';
