/*
 * Compatibility wrapper for legacy `src/shared/ipc/envelope` imports.
 * Re-exports the canonical envelope factory from
 * `src/shared/nerv/envelope.js`.
 */

module.exports = require('../nerv/envelope');
