// @ts-check - Type checking rigoroso habilitado (arquivo core)
import * as engine from './engine/context_engine.js';

/**
 * Atalho de export para o resolvedor de contexto canônico do engine.
 */
export const resolveContext = engine.resolveContext;
