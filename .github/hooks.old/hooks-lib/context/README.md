# context

Domínio reservado para helpers de contexto transacional e reconciliação de sessão.

Naming local:

- Helpers devem usar prefixos semânticos (`ctx_`, `session_`) e não usar sufixo `-lib` quando não forem wrappers de script.
