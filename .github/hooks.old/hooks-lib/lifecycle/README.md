# lifecycle

Wrappers e helpers para sessão/seção/turno/subturno e scripts de lifecycle.

Naming local:

- Wrappers de script: `<script>-lib.sh`.
- Entrypoints canônicos de migração lifecycle: `session-*-core.sh` e `session-*-aux.sh`.
