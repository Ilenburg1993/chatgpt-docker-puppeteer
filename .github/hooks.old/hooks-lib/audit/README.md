# audit

Wrappers e helpers para emissão de eventos, relatórios e sincronização de trilhas de auditoria.

Naming local:

- Wrappers de script: `<script>-lib.sh`.
- Helpers de auditoria devem preferir prefixos `audit_` ou verbos explícitos (`emit_`, `sync_`, `generate_`).
