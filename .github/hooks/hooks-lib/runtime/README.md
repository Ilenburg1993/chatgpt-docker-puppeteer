# runtime

Domínio reservado para helpers de runtime (`input`, `path`, `lock`, `filesystem helpers`).

Naming local:

- Wrappers de script: `<script>-lib.sh`.
- Helpers canônicos de runtime: nomes curtos sem `-lib` (ex.: `common.sh`, `config.sh`).
