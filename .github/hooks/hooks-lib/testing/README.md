# testing

Verificadores estruturais e suporte aos fluxos de smoke por domínio.

Naming local:

- Wrappers de script: `<script>-lib.sh`.
- Scripts verificadores associados devem manter semântica explícita no nome (`verify-*`, `export-*`, `smoke-*`).
