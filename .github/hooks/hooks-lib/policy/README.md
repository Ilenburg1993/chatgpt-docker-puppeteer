# policy

Helpers de autorização, governança Template A/D/E/F/G, reason-codes e validações de fechamento.

Naming local:

- Wrappers de script: `<script>-lib.sh` (ex.: `reset-auth-violation-lib.sh`).
- Módulo canônico de policy: `policy.sh`.
