# =============================================================================
# .env.example — ChatGPT Docker Puppeteer (Runtime JS)
# =============================================================================
#
# Este arquivo documenta variáveis OPCIONAIS usadas pela aplicação Node.js.
#
# IMPORTANTE:
# • O DevContainer e o Docker já configuram o ambiente base
# • Este arquivo NÃO é obrigatório
# • Use apenas se a aplicação realmente ler process.env
#
# =============================================================================

# ---------------------------------------------------------------------------
# Ambiente da aplicação
# ---------------------------------------------------------------------------
NODE_ENV=development
APP_PORT=3008
LOG_LEVEL=debug

# Versão lógica da aplicação (informativa)
APP_VERSION=dev

# ---------------------------------------------------------------------------
# Chrome remoto (HOST — Windows)
# ---------------------------------------------------------------------------
# Chrome deve estar rodando no Windows com:
#   chrome.exe --remote-debugging-port=9222 --user-data-dir=%TEMP%\chrome-debug
#
CHROME_REMOTE_HOST=host.docker.internal
CHROME_REMOTE_PORT=9222

# Timeout e tentativas de conexão
CHROME_CONNECTION_TIMEOUT=5000
CHROME_CONNECTION_RETRIES=3

# ---------------------------------------------------------------------------
# Puppeteer
# ---------------------------------------------------------------------------
# Chromium do container é apenas compatibilidade técnica
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# ---------------------------------------------------------------------------
# Diretórios lógicos (relativos ao workspace)
# ---------------------------------------------------------------------------
QUEUE_DIR=./fila
RESPONSE_DIR=./respostas
LOG_DIR=./logs
PROFILE_DIR=./profile

# ---------------------------------------------------------------------------
# Performance / controle
# ---------------------------------------------------------------------------
MAX_WORKERS=3
TASK_TIMEOUT=30000
MAX_RETRIES=3

# ---------------------------------------------------------------------------
# Localização
# ---------------------------------------------------------------------------
TZ=America/Sao_Paulo

# ---------------------------------------------------------------------------
# Flags experimentais (opcional)
# ---------------------------------------------------------------------------
DEBUG=false
EXPERIMENTAL_FEATURES=false

# =============================================================================
# NOTAS
# =============================================================================
#
# • Copie para .env SOMENTE se necessário:
#     cp .env.example .env
#
# • Nunca commite .env
# • Se não existir .env, o sistema continua funcionando
#
# =============================================================================
