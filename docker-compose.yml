version: '3.8'

services:
  # =============================================================================
  # Main Agent Service
  # Requires Chrome running on host with remote debugging:
  # "C:\Program Files\Google\Chrome\Application\chrome.exe"
  #   --remote-debugging-port=9222
  #   --user-data-dir="C:\chrome-automation-profile"
  # =============================================================================
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatgpt-agent
    restart: unless-stopped
    
    environment:
      - NODE_ENV=production
      - TZ=America/Sao_Paulo
      # Chrome remote debugging connection
      # host.docker.internal works on Docker Desktop (Windows/Mac)
      # For Linux, use: --add-host=host.docker.internal:host-gateway
      - CHROME_WS_ENDPOINT=ws://host.docker.internal:9222
      # Optional settings
      # - MAX_RETRIES=3
      # - LOG_LEVEL=info
    
    volumes:
      # Persistent data
      - ./fila:/app/fila
      - ./respostas:/app/respostas
      - ./logs:/app/logs
      - ./profile:/app/profile
      # Optional: Mount configs for hot-reload
      # - ./config.json:/app/config.json:ro
      # - ./dynamic_rules.json:/app/dynamic_rules.json:ro
    
    ports:
      - "3008:3008"
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3008/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        compress: "true"
    
    # Resource limits (optional but recommended)
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    
    # Networking
    networks:
      - agent-network

  # =============================================================================
  # Optional: Development Service
  # Requires Chrome with remote debugging on host
  # =============================================================================
  agent-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: chatgpt-agent-dev
    profiles:
      - dev
    
    environment:
      - NODE_ENV=development
      - TZ=America/Sao_Paulo
      - CHROME_WS_ENDPOINT=ws://host.docker.internal:9222
    
    volumes:
      # Mount entire source for hot reload
      - .:/app
      - /app/node_modules
      - ./fila:/app/fila
      - ./respostas:/app/respostas
      - ./logs:/app/logs
      - ./profile:/app/profile
    
    ports:
      - "3008:3008"
      - "9229:9229"  # Node.js debugger port
    
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
