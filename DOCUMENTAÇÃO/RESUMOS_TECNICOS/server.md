# src/server

O Server provê API, dashboard web e integração em tempo real via Express e Socket.io. Gerencia rotas, middleware, supervisão, telemetria, streams e adaptação NERV para comunicação com o frontend. Centraliza monitoramento, controle e exposição dos serviços do agente.
