# CODEX_AUDIT_2026-02-21_03-33

- Data/hora (UTC): 2026-02-21T03:33:04Z
- Onda: Wave 14 — Fluxo Integrado (Kernel -> Fila -> DriverPool/BrowserPool -> Missoes)
- Prioridade: bug-first (`P0/P1`)
- Status da rodada: concluida e validada

## Baseline da rodada

- Parse/import dos entrypoints: OK, com side effects de log em import puro no baseline inicial.
- PM2/MCP/RAG/LSP: operacionais.
- Regressao Wave12/Wave13: verde no inicio.

## Achados Wave 14 tratados

1. `CODX-FLOW-001` (`P0`): hot-reuse com `taskId` stale no lifecycle monitor.
2. `CODX-FLOW-002` (`P1`): envio de abort sem `await` no TaskControlWatcher.
3. `CODX-FLOW-003` (`P1`): side effects de import no caminho `main/config/env`.
4. `CODX-FLOW-004` (`P1`): validação de domínio por `includes` no TargetDriver.
5. `CODX-FLOW-005` (`P1`): timeout sem cancelamento explícito no adapter.
6. `CODX-FLOW-006` (`P2`): factory sem detach explícito de browserPool por options.

## Implementacao por fase

### Fase 1 (P0) — Hot Pool + Lifecycle consistency

- `src/infra/browser_pool/PageLifecycleMonitor.js`
  - Adicionado `rebindTaskId(newTaskId)` com telemetria de transição.
  - Handlers close/disconnect passaram a remover com fallback por `page`.
- `src/infra/browser_pool/pool_manager.js`
  - `updatePageTaskId()` agora rebinda monitor explicitamente.
  - `removePageFromPool(taskId, pageRef)` com fallback por referência da página.

### Fase 2 (P1) — Abort control path

- `src/agent/task_control_watcher.js`
  - `sendCommand` agora aguardado via `_withTimeout` com retry curto.
  - Novos knobs: `TASK_CONTROL_ABORT_TIMEOUT_MS` (default 1500), `TASK_CONTROL_ABORT_MAX_RETRIES`
    (default 2).
  - Evento de falha persistente: `CONTROL_ABORT_FAILED`.

### Fase 3 (P1) — Import-safety

- `src/core/env_bootstrap.js`
  - `dotenv.config` em modo silencioso (`quiet`) + bootstrap idempotente.
- `src/core/config.js`
  - Validação/depreciação de env removidas do top-level; execução no `reload()`.
- `src/main.js`
  - Removido import top-level de `./core/env_bootstrap.js`.

### Fase 4 (P1) — Domain hardening

- `src/core/domain_matcher.js` (novo helper compartilhado).
- `src/driver/core/TargetDriver.js`
  - Domínio agora validado por hostname exato/subdomínio.
- `src/infra/browser_pool/PageValidator.js`
  - Reuso do mesmo helper para contrato único.

### Fase 5 (P1/P2) — Timeout + detach hardening

- `src/driver/nerv_adapter/driver_nerv_adapter.js`
  - Substituição de helper legado por `_withTimeout` cancelável em todo o fluxo.
- `src/driver/factory.js`
  - `start({ browserPool: null })` agora detach explícito.
  - Novo `clearBrowserPool()`.

### Fase 6 — Auditoria contínua

- `scripts/audit/collectors/performance.mjs`
  - Nova regra para detectar `Promise.race` + timeout sem cancelamento.
- `contracts/domains/runtime.json`
  - Adicionados contratos:
    - `CONTRACT-RUNTIME-ABORT-COMMAND-AWAIT`
    - `CONTRACT-RUNTIME-IMPORT-SIDE-EFFECT`
    - `CONTRACT-RUNTIME-HOT-POOL-MONITOR-CONSISTENCY`

## Testes executados

- Parse/import:
  - `node --check ...` (arquivos-alvo Wave14) -> OK
  - `env -u NO_COLOR -u FORCE_COLOR node --input-type=module -e "import './src/main.js'; console.log('OK')"`
    -> OK
  - `env -u NO_COLOR -u FORCE_COLOR node --input-type=module -e "import './src/server/main.js'; console.log('OK')"`
    -> OK
  - `env -u NO_COLOR -u FORCE_COLOR node --input-type=module -e "import './src/driver/factory.js'; console.log('OK')"`
    -> OK
- Regressao existente:
  - `node --test tests/regression/test_wave3_runtime_hardening.spec.js` -> 1/1 pass
  - `node --test tests/regression/test_wave12_entrypoint_import_resilience.spec.js tests/regression/test_wave12_main_signal_portability.spec.js tests/regression/test_wave13_*.spec.js tests/integration/driver/test_wave13_hot_pool_reuse_integrity.spec.js tests/integration/server/test_split_boot_retry.spec.js`
    -> 13/13 pass
- Novos testes Wave14:
  - `node --test tests/regression/test_wave14_hot_pool_monitor_taskid_rebind.spec.js tests/regression/test_wave14_task_control_watcher_abort_await.spec.js tests/regression/test_wave14_entrypoint_import_no_side_effect_logs.spec.js tests/regression/test_wave14_targetdriver_domain_strict_match.spec.js tests/regression/test_wave14_driver_adapter_timeout_cleanup.spec.js`
    -> 15/15 pass
- Operação:
  - `npm run audit:preflight` -> OK
  - `npm run audit:quick` -> OK (`run_id=WAVE_AUDIT_QUICK_2026-02-21T03-32-36-214Z`, findings=0)
  - `npm run daemon:status` -> OK
  - `npm run mcp:diagnose` -> OK
  - `npm run rag:health -- --json` -> OK
  - `npm run lsp:health -- --json` -> OK

## Rollback por fase

1. `Fase 6` -> rebaixar contratos para `warn` e manter coleta.
2. `Fase 5` -> reverter helper de timeout/detach mantendo Fases 1-4.
3. `Fase 4` -> reverter apenas `TargetDriver` se compatibilidade imediata exigir.
4. `Fase 3` -> restaurar validação top-level somente em incidente crítico de bootstrap.
5. `Fase 2` -> retornar fluxo de abort simples sem retry/timeout (temporário).
6. `Fase 1` -> rollback isolado de rebind/fallback caso haja regressão operacional.

## Estado final

- Wave 14 aplicada com gates verdes.
- Entrypoints/factory em import puro sem ruído operacional.
- Fluxo de taskId no hot pool consistente com lifecycle monitor.
- Contratos/coletores atualizados para vigilância contínua das classes de bug tratadas.
