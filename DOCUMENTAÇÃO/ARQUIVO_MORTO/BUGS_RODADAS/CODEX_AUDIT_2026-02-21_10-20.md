# CODEX_AUDIT_2026-02-21_10-20

- Timestamp: 2026-02-21T10:20:30Z
- Onda: Wave 19 — Upgrade Vasto Frontend (Hard Cutover) + Backend Evolutivo
- Escopo: frontend vNext (`dashboard-ui`) + control plane mission create + contratos/auditoria
- Política: bug-first (`P0/P1`)

## Baseline

### Início

- PM2: online (`agente-gpt`, `dashboard-web`, `chrome-proxy`)
- MCP: `ok=true`, `tools_count=14`
- RAG: `ok=true`, `available=true`
- LSP: `ok=true`, `lsp_tools_present=true`, `lsp_functional_ok=true`

### Fim

- PM2: online (sem degradação nova)
- MCP/RAG/LSP: saudáveis
- `audit:preflight`: `ok=true`
- `audit:quick`: `findings=0`, `shadow_would_block=false`

## Achados e Correções Aplicadas

1. `CODX-W19-001` (`P0`) criação de missão fora do command flow.
   - Correção: `MISSION_CREATE` em `src/server/domain/control_command_service.js` +
     `createMissionCommand` em `src/server/domain/mission_control_service.js`.
2. `CODX-W19-002` (`P1`) comandos críticos sem confirmação forte.
   - Correção: `src/dashboard-ui/src/lib/command_guard.js` + integração em `TasksView`,
     `TaskDetail`, `MissionDetail`, `Missions`.
3. `CODX-W19-003` (`P1`) realtime suscetível a burst/duplicação.
   - Correção: buffer + dedupe + cursor guard em
     `src/dashboard-ui/src/composables/useSsotRealtime.js` e
     `src/dashboard-ui/src/stores/events_vnext.js`.
4. `CODX-W19-004` (`P1`) índice cruzado task↔mission incompleto.
   - Correção: `taskIdsByMissionId` e getters cruzados em
     `src/dashboard-ui/src/stores/tasks_vnext.js`.
5. `CODX-W19-005` (`P1`) falta de contratos de regressão para `/api/missions` e dedupe realtime.
   - Correção: novos contratos em `contracts/domains/architecture.json` e
     `contracts/domains/runtime.json` + coletores atualizados.

## Testes Executados

- `node --test tests/regression/test_wave19_*.spec.js` -> `7/7 pass`
- `node --test tests/regression/test_wave18t_dashboard_views_no_direct_tasks_mutation_calls.spec.js tests/regression/test_wave18_control_commands_single_entrypoint.spec.js tests/integration/server/test_wave18t_dashboard_tasks_enriched_mission_context.spec.js tests/integration/server/test_wave18_dashboard_realtime_control_command_status.spec.js`
  -> `4/4 pass`
- `node --test tests/integration/server/test_wave18t_task_detail_command_flow_only.spec.js tests/regression/test_wave18t_bulk_reassign_preview_and_validation.spec.js tests/regression/test_wave18t_task_patch_cannot_change_mission_id.spec.js tests/regression/test_wave18t_task_reassign_requires_paused_or_ready.spec.js`
  -> `4/4 pass`
- `npm --prefix src/dashboard-ui run build` -> `OK`

## Rollback por Fase

1. Reverter views/stores vNext da fase atual mantendo control plane e contratos.
2. Reativar compat frontend apenas se bloquear operação.
3. Preservar governança (`tracker`, `snapshot`, `artifacts`) em qualquer rollback.

## Próximo Escopo Fechado

1. Remoção física/arquivamento dos módulos legacy não roteados.
2. Dry-run obrigatório (`/api/control/validate`) para comandos críticos antes do submit final.
3. Promoção progressiva de contratos Wave 19 (`warn` -> `block`) após baseline estável.
