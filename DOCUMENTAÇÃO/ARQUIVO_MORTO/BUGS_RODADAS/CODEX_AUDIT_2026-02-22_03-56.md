# Snapshot — Wave AQ5 (2026-02-22 03:56)

## Foco

- Testes de parser/fallback do `collect-quality`
- Promoção seletiva de contracts quality para `p1`
- Validação do `audit:quick` após rollout

## Entregas

1. Parsers internos de `scripts/audit/collectors/quality.mjs` exportados para testes unitários.
2. Novos testes unitários:
   - `test_quality_parsers_eslint_json.spec.js`
   - `test_quality_parsers_prettier_check.spec.js`
   - `test_quality_collector_fallback_rules.spec.js`
3. `test:unit:audit-quality` expandido para cobrir 14 testes.
4. `contracts/domains/quality.json` com rollout seletivo `p1`:
   - `CONTRACT-QUALITY-NODE-SYNTAX`
   - `CONTRACT-QUALITY-TYPECHECK-NODE`
   - `CONTRACT-QUALITY-TYPECHECK-BROWSER`
   - `CONTRACT-QUALITY-TS-IGNORE-FORBIDDEN` (demais permanecem em `warn`)

## Validação

- `npm run -s test:unit:audit-quality`: **OK (14/14)**
- `npm run -s typecheck:full`: **OK**
- `npm run -s lint -- --quiet`: **OK**
- `check:forbidden` (hybrid parity): `registry_errors=[]`, `parity_mismatches=0`
- `audit:quick` após rollout: `WAVE_AUDIT_QUICK_2026-02-22T06-54-14-816Z`
  - `gate_blocking=false`
  - `shadow_would_block=false`
  - `quality_gates`: `node_check_ok/typecheck_node_ok/typecheck_browser_ok/ts_ignore_ok = true`
  - violações remanescentes em `warn`: `prettier`, `jsdoc_delta`

## Nota operacional

- O quick caiu em `strategy=full` por branch/config de alto risco e invalidou parte do cache nesta
  rodada; rollout de contracts críticos `p1` permaneceu estável e sem bloqueio.
