# CODEX_AUDIT_2026-02-21_02-44

- Data (UTC): 2026-02-21T02:44:09Z
- Escopo: BrowserPool + DriverPool + integracoes diretas (`main`, `driver adapter`, `config`,
  `monitor`)
- Politica: hard-refactor now, bug-first (`P0/P1`)

## Baseline Operacional

- PM2: `agente-gpt`, `dashboard-web`, `chrome-proxy` online
- MCP: `mcp:diagnose` OK (`tools/list=14`)
- RAG: `rag:health ok=true` (risco de frescor residual)
- LSP: funcional (`preflight` com `lsp ok=true`)

## Achados desta rodada (IDs)

1. `CODX-BPDP-001` (`P0`) side-effect de import/lifecycle implícito em factory.
2. `CODX-BPDP-002` (`P0`) hot-pool cleanup quebrando reuso.
3. `CODX-BPDP-003` (`P0`) retry recursivo em `allocate`.
4. `CODX-BPDP-004` (`P1`) eventos não declarados no monitor.
5. `CODX-BPDP-005` (`P1`) reconexão parcial + concorrência no pool.
6. `CODX-BPDP-006` (`P1`) validação de domínio por `includes`.
7. `CODX-BPDP-007` (`P1`) schema/getters BrowserPool incompletos.

## Correções Aplicadas

- `src/driver/factory.js`: `start()/ensureReady()`, import-safe, serialização por target, waiters
  FIFO, hardening de recycle hot.
- `src/core/environment_resolver.js`: targets via metadata runtime (`getAvailableTargets`).
- `src/main.js`: `driverFactory.start(...)` explícito + parse normalizado de config BrowserPool.
- `src/driver/nerv_adapter/driver_nerv_adapter.js`: cleanup hot/cold correto.
- `src/infra/browser_pool/pool_manager.js`: retry iterativo limitado, reconexão atualiza todas as
  entradas, single-flight health checks, `updatePageTaskId` robusto.
- `src/infra/browser_pool/PeriodicHealthMonitor.js`: enum completo + single-flight `runHealthCheck`.
- `src/infra/browser_pool/PageValidator.js`: hostname exato/subdomínio.
- `src/core/config.js`: schema/getters BrowserPool formalizados.

## Novos testes da onda

- `tests/regression/test_wave13_driver_factory_import_no_side_effect.spec.js`
- `tests/integration/driver/test_wave13_hot_pool_reuse_integrity.spec.js`
- `tests/regression/test_wave13_browserpool_allocate_retry_non_recursive.spec.js`
- `tests/regression/test_wave13_browserpool_reconnection_updates_all_entries.spec.js`
- `tests/regression/test_wave13_periodic_monitor_event_contract.spec.js`

## Validação

- Parse/import: OK
- Regressão existente (wave12/wave3/split retry): 6/6 pass
- Wave13: 7/7 pass (5 arquivos; 2 testes extras no monitor + 2 no hot/cold cleanup)
- `audit:preflight`: OK
- `audit:quick`: OK (`WAVE_AUDIT_QUICK_2026-02-21T02-43-28-339Z`, findings=0)

## Rollback

1. Runtime: `Fase 5 -> Fase 4 -> Fase 3 -> Fase 2 -> Fase 1`.
2. Preservar artefatos de governança (tracker/snapshot).
3. Incidente de hot-pool: `DRIVER_HOT_POOL_ENABLED=false`.

## Risco Residual

- RAG com risco de frescor de índice (não bloqueador para esta onda).
