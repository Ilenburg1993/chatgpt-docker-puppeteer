# Snapshot — Wave AQ3 (2026-02-22 03:40)

## Foco
- Cache local por step no `collect-quality`
- Paralelismo controlado em grupos seguros
- Deduplicação de findings no collector
- Telemetria de cache/paralelismo/dedup serializada no `audit_report`

## Entregas
1. `scripts/audit/collectors/quality.mjs` com cache por step (`artifacts/audit/cache/quality`) para `lint`, `typecheck_*`, `prettier`, `jsdoc_*`, `ts_ignore_scan`.
2. Paralelismo controlado (`quick-smoke`, `delta-docs-scan`) com rollback por `--quality-parallelism serial`.
3. Deduplicação de findings no `collect-quality` com fingerprint estável e telemetria `dedup`.
4. `scripts/audit/runner.mjs` ajustado para persistir `quality_execution.cache`, `quality_execution.parallelism` e `quality_execution.dedup` no report.
5. `scripts/audit/lib/schema.mjs` atualizado (campos aditivos da telemetria quality).

## Validação
- `node --check scripts/audit/collectors/quality.mjs scripts/audit/runner.mjs scripts/audit/lib/schema.mjs`: OK
- `typecheck:full`: OK
- `lint --quiet`: OK
- `audit:quick` (AQ3 evidência de performance):
  - `WAVE_AUDIT_QUICK_2026-02-22T06-38-06-108Z` -> cache miss após patch de runner (`hits=0`, `misses=6`, `writes=6`), `duration_ms_total=99870`
  - `WAVE_AUDIT_QUICK_2026-02-22T06-39-58-895Z` -> cache hit (`hits=6`, `misses=0`, `writes=0`), `duration_ms_total=23091`
- Telemetria confirmada no report:
  - `quality_execution.cache`
  - `quality_execution.parallelism`
  - `quality_execution.dedup`

## Nota operacional
- O smart-hybrid permaneceu em `strategy=full` nesta validação por branch/config de alto risco; mesmo assim o cache reduziu fortemente o custo dos steps caros na segunda execução.
