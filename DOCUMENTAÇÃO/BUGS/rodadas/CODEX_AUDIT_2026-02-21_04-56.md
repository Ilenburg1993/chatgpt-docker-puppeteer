# CODEX AUDIT SNAPSHOT — Wave 16R

- Data/hora: 2026-02-21 04:56 (local)
- Onda: Wave 16R — Replano Consolidado Dashboard/Server
- Bug IDs: `CODX-DS16R-001` a `CODX-DS16R-008`
- Escopo: dashboard/server + contratos/coletores + envs + UI auth/socket

## Baseline Inicial
- PM2: online (`agente-gpt`, `dashboard-web`, `chrome-proxy`)
- MCP/LSP: degradado por caminho inválido de script (`scripts/diagnose-mcp.mjs`, `scripts/diagnose-lsp.mjs` ausentes)
- RAG: saudável (`ok=true`, `available=true`)

## Correções Aplicadas
1. Auth strict dashboard sem hardcode de credenciais em `src/server/api/controllers/dashboard.js`.
2. Remoção de side effect de import (timer) e lazy-load de telemetria no controller dashboard.
3. Logout endurecido com `authenticate` obrigatório.
4. Socket hardening em `src/server/engine/socket.js`:
   - JWT obrigatório por default em handshake dashboard.
   - `dashboard:command` desabilitado por default (`COMMAND_CHANNEL_DISABLED`).
   - role gate para comando (`DASHBOARD_COMMAND_ROLE`).
   - CORS allowlist unificada via `CONFIG.ALLOWED_ORIGINS` + `DASHBOARD_ORIGIN`.
5. Boot/shutdown de token blocklist cleanup:
   - start no bootstrap (`src/server/main.js`)
   - stop no lifecycle (`src/server/engine/lifecycle.js`)
6. Realtime single-source:
   - `DASHBOARD_TASK_SYNC_MODE=ssot_feed` default
   - bridge legacy inicializa apenas em `legacy_bridge`
7. Frontend consolidation:
   - `useAuth` singleton/shared state + login/logout centralizados.
   - `LoginModal` usa composable (remove fetch duplicado).
   - interceptor Bearer no `src/dashboard-ui/src/lib/http.js`.
   - `useSocket` com estado reativo compartilhado e auth no handshake.
   - `SystemHealth.vue` desacoplado de `useRealtime` legado.
8. Contratos/coletores:
   - novos contratos arquitetura/runtime para dashboard auth/import/socket/dual-feed.
   - coletores atualizados para sinais `dashboard_auth_insecure`, `dashboard_socket_command_unsafe`, `dashboard_dual_feed`.
9. Scripts operacionais:
   - `package.json` corrigido para `scripts/health/diagnose-mcp.mjs` e `scripts/health/diagnose-lsp.mjs`.

## Validação
- Parse/check: OK (arquivos alvo da wave).
- Import safety:
  - `import './src/server/api/controllers/dashboard.js'` -> OK (sem timer blocklist ativo).
  - `import './src/main.js'` -> OK
  - `import './src/server/main.js'` -> OK
  - `import './src/driver/factory.js'` -> OK
- Testes:
  - `node --test` com suite alvo dashboard/server + novos testes wave16r -> **19/19 pass**.
- Auditoria:
  - `npm run audit:preflight` -> `ok=true`
  - `npm run audit:quick` -> `WAVE_AUDIT_QUICK_2026-02-21T07-51-54-826Z`, `findings=0`, `shadow_would_block=false`.
- Runtime final:
  - PM2: online
  - MCP: OK (`tools/list count=14`)
  - RAG: OK (`updated_at_iso=2026-02-15T13:28:32.814Z`)
  - LSP: OK (`lsp_functional_ok=true`)

## Riscos Residuais
1. `scripts/check_forbidden_patterns.js --contracts-mode hybrid --parity-mode` mantém alta divergência/paridade de contratos legados (não bloqueante desta wave, risco conhecido da trilha v3).

## Rollback
1. Runtime flags imediatas:
   - `DASHBOARD_AUTH_REQUIRED=false`
   - `DASHBOARD_SOCKET_AUTH_REQUIRED=false`
   - `DASHBOARD_COMMANDS_ENABLED=true` (somente contingência)
   - `DASHBOARD_TASK_SYNC_MODE=legacy_bridge`
   - `DASHBOARD_EMIT_TASK_UPDATED_COMPAT=true`
2. Ordem por fase: F7 -> F6 -> F5 -> F4 -> F3 -> F2 -> F1.
3. Preservar governança/documentação (F0) em qualquer rollback.
