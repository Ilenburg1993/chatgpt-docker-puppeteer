# CODEX_AUDIT Snapshot — 2026-02-21 04:11 UTC

- Onda: Wave 14.1 — Consolidacao Kernel SSOT (Task/Attempt/Mission) + Hardening de Fluxo
- Tracker canonico: `DOCUMENTAÇÃO/BUGS/CODEX_AUDIT_TRACKER.md`
- Run principal de auditoria: `WAVE_AUDIT_QUICK_2026-02-21T04-10-08-702Z`
- Politica: bug-first (`P0/P1` antes de backlog tecnico)

## Baseline Operacional
### Inicio
- PM2: `agente-gpt`, `dashboard-web`, `chrome-proxy` online.
- MCP: `mcp:diagnose` OK, `tools/list count=14`, LSP tools presentes.
- RAG: `ok=true`, `available=true` (risco residual de frescor mantido).
- LSP: `ok=true`, funcional.

### Fim
- PM2: manteve 3 processos online.
- MCP: manteve `OK` com `lsp_tools_present=true` e `lsp_functional_ok=true`.
- RAG: manteve `ok=true`, `available=true`, indice em `2026-02-15T13:28:32Z`.
- LSP: manteve `ok=true`.

## Bug IDs da Rodada
- `CODX-W141-001`: Kernel `NEXT_STEP` fora de SSOT parcial.
- `CODX-W141-002`: Missao com caminho paralelo de dispatch direto.
- `CODX-W141-003`: Eventos stale de attempt podendo mutar status/lock.
- `CODX-W141-004`: Validacao de dominio fraca no `DriverReadinessGuard`.
- `CODX-W141-005`: Ausencia de deteccao automatica ownership/lock-causality.
- `CODX-W141-006`: Coerencia de estado de missao no DB durante enqueue SSOT.
- `CODX-W141-007`: Release de lock sem guarda causal em partes do fluxo.

## Correcoes Implementadas
1. Builder SSOT unico de next-step (`src/agent/workflow_next_step_builder.js`).
2. `kernel_nerv_bridge` criando child task com `insertTask(ifNotExists)` e evento padrao de auditoria.
3. `MissionManager` default para `ssot_queue` com fallback temporario `legacy_direct`.
4. Invariantes de `task/attempt/lock` centralizados (`src/agent/task_attempt_invariants.js`) e aplicados em `queue_worker`/`task_state_projector`.
5. `task_repo.releaseTaskLock` com `expectedAttemptId` opcional para causalidade.
6. Dominio estrito em `DriverReadinessGuard` via `isDomainMatch`.
7. Contratos/collectors novos para ownership SSOT e causalidade lock-attempt em modo `warn`.

## Validacao Executada
- `node --check ...` nos arquivos alterados -> OK.
- `import './src/main.js'`, `import './src/server/main.js'`, `import './src/driver/factory.js'` -> OK (saida limpa).
- `node --test tests/regression/test_wave12_entrypoint_import_resilience.spec.js tests/regression/test_wave14_*.spec.js tests/unit/agent/test_ssot_orchestration_worker.spec.js tests/integration/test_mission_system_integration.spec.js` -> `36/36 pass`.
- `node --test tests/regression/test_wave15_*.spec.js` -> `8/8 pass`.
- `npm run audit:preflight` -> `ok: true`.
- `npm run audit:quick` -> findings `0`, `shadow_would_block=false`.

## Rollback por Fase
1. Reverter em ordem: F6 -> F5 -> F4 -> F3 -> F2 -> F1.
2. Preservar F0 (governanca/tracker) em qualquer incidente.
3. Kill-switch operacional: `MISSION_STEP_DISPATCH_MODE=legacy_direct` se necessario.

## Proxima Onda Recomendada
1. Promover contratos de ownership/lock-causality de `warn` para `block` com baseline limpa.
2. Remover fallback `legacy_direct` se estabilidade persistir.
3. Expandir testes de reprocessamento/duplicacao em workflow mission-centric com carga concorrente.
