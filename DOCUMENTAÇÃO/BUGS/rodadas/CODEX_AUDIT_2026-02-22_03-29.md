# Snapshot — Wave AQ2 (2026-02-22 03:29)

## Foco
- Contracts v3 de quality gates (`warn`)
- Vínculo de findings do `collect-quality` aos contratos
- Threshold agregado de `jsdoc_full` para `audit:deep/nightly`

## Entregas
1. `contracts/domains/quality.json` criado (10 contratos de quality).
2. `contracts/registry.json` atualizado (domínio `quality`).
3. `scripts/audit/collectors/quality.mjs` emite `contract_id` + telemetria JSDoc (`delta/full/threshold`).
4. `scripts/audit/runner.mjs` com flag `--quality-jsdoc-full-threshold-pct` e persistência de `quality_execution.jsdoc`.
5. `scripts/audit/lib/schema.mjs` atualizado (campo aditivo `quality_execution.jsdoc`).

## Validação
- `registry.errors=[]` no `check:forbidden` (hybrid parity)
- `typecheck:full`: OK
- `lint --quiet`: OK
- `audit:quick` (`--triage false`): OK
- `contract_coverage.quality`: presente e coberto no run
- `jsdoc:coverage:json`: OK (coverage global 32.3%)

## Nota operacional
- O threshold default de `jsdoc_full` (80%) é intencionalmente conservador e atualmente ficará abaixo do baseline; contratos permanecem em `warn` nesta fase.
