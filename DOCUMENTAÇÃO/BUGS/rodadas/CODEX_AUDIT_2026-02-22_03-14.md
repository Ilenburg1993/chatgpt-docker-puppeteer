# CODEX_AUDIT_2026-02-22_03-14

- Data/hora (UTC): 2026-02-22T03:14:08Z
- Onda: Wave 20B — Hardening de Boot e Subprocessos (PM2-first, SSOT de Lifecycle)
- Bug IDs: `CODX-BOOT20B-001..012`
- Run de auditoria: `WAVE_AUDIT_QUICK_2026-02-22T03-13-11-382Z`

## Baseline inicio
1. `npm run daemon:status` -> PM2 saudavel (3/3 online).
2. `npm run mcp:diagnose -- --json` -> MCP saudavel (`ok=true`, `tools_count=14`).
3. `npm run rag:health -- --json` -> RAG saudavel (`ok=true`, `available=true`).
4. `npm run lsp:health -- --json` -> LSP funcional (`ok=true`).

## Implementacao consolidada
1. Import-safety deterministico
- `src/core/entrypoint_guard.js` criado.
- `src/main.js` sem auto-bootstrap por `DAEMON_MODE`.
- `src/server/main.js` sem bootstrap por `NODE_APP_INSTANCE` em import indireto.

2. Retry/backoff unificado
- `src/core/retry_policy.js` criado.
- split connect do `main` migrado para retry exponencial com jitter.

3. Ownership unico de sinais/shutdown
- `scripts/chrome-proxy-service.js` com `AUTO_HANDLE_SIGNALS=false` no service.
- Removido cleanup agressivo de listeners globais no wrapper.
- `src/integration/mcp/upstream-manager.mjs` com hooks globais opcionais (`installShutdownHook`).

4. SSOT de lifecycle por recurso
- `src/core/runtime_resource_registry.js` criado.
- `src/server/main.js` registra recursos runtime com owner `dashboard-web`.
- `src/server/engine/lifecycle.js` para shutdown reverso por recurso + timeout.

5. Readiness granular
- `src/server/engine/app.js` expande `/ready` com `runtime_resources` (`ready|degraded|not-ready`) e componentes degradados.

6. Config/contratos/coletores
- `src/core/config.js` + `.env.schema.json` + `.env.*` com novas envs de boot.
- contratos adicionados em `contracts/domains/architecture.json` e `contracts/domains/runtime.json`.
- coletores atualizados em `scripts/audit/collectors/runtime.mjs` e `scripts/audit/collectors/performance.mjs`.

7. Testes novos Wave 20B
- `tests/regression/test_wave20b_server_main_import_pm2_env_no_boot.spec.js`
- `tests/regression/test_wave20b_main_import_daemon_env_no_boot.spec.js`
- `tests/regression/test_wave20b_signal_ownership_single_owner.spec.js`
- `tests/regression/test_wave20b_upstream_manager_no_global_hooks_when_delegated.spec.js`
- `tests/integration/server/test_wave20b_ready_degraded_components_contract.spec.js`

## Validacao
1. Parse/check alvo -> OK.
2. Import-safety (normal + PM2 env simulada + DAEMON_MODE) -> OK.
3. Suite obrigatoria regressao + wave20b -> 16/16 pass.
4. `npm run audit:preflight` -> ok=true.
5. `npm run audit:quick` -> sucesso, `findings=2`, `shadow_would_block=false`.
6. `node scripts/check_forbidden_patterns.js --json --contracts-mode hybrid --parity-mode` -> executado (findings historicos fora do escopo permanecem).

## Baseline fim
1. PM2: saudavel.
2. MCP: saudavel.
3. RAG: saudavel.
4. LSP: funcional.

## Risco residual
1. Warning `NO_COLOR` x `FORCE_COLOR` persiste em comandos `node --test` quando o ambiente pai exporta ambos.
2. Backlog grande de findings de contratos em dominios nao cobertos nesta wave (enforcement em `warn`).

## Rollback por fase
1. Ordem: F8 -> F7 -> F6 -> F5 -> F4 -> F3 -> F2 -> F1.
2. Preservar F0 (governanca/evidencia).
3. Em incidente:
- F1: restaurar guard antigo temporariamente com flag explicita.
- F2: manter owner antigo apenas no processo afetado.
- F4/F5: manter readiness/retry anterior sem desfazer import-safety.
