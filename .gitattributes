# =============================================================================
# .gitattributes — Normalização de Quebras de Linha (Line Endings)
# Version: 2.1 FIXED (23/01/2026)
# =============================================================================
# PROPÓSITO:
# Garantir que todos os arquivos de texto usem LF (Unix Style),
# independentemente de quem os edita (Windows, Linux, Mac).
# Isso previne erros de execução dentro do Docker/Linux.
# =============================================================================

# 1. REGRA GLOBAL (O mais importante)
# Detecta arquivos de texto e força LF na hora de salvar no Git.
# No checkout, mantém LF (ideal para WSL2 e Containers).
* text=auto eol=lf

# 2. SCRIPTS E EXECUTÁVEIS (CRÍTICO)
# Estes arquivos JAMAIS devem ter CRLF, senão o Linux falha.
*.sh      text eol=lf
*.bash    text eol=lf
*.zsh     text eol=lf
*.js      text eol=lf
*.mjs     text eol=lf
*.ts      text eol=lf
*.py      text eol=lf
*.pl      text eol=lf
*.rb      text eol=lf

# 3. ARQUIVOS DE CONFIGURAÇÃO DOCKER
Dockerfile* text eol=lf
docker-compose*.yml     text eol=lf
docker-compose*.yaml    text eol=lf
.dockerignore           text eol=lf

# 4. CONFIGURAÇÕES VS CODE E EDITOR
.gitattributes          text eol=lf
.gitignore              text eol=lf
.editorconfig           text eol=lf
*.json                  text eol=lf
*.jsonc                 text eol=lf
*.xml                   text eol=lf
*.yaml                  text eol=lf
*.yml                   text eol=lf
*.toml                  text eol=lf
*.ini                   text eol=lf
*.env                   text eol=lf
*.env.* text eol=lf
.eslintrc               text eol=lf
.prettierrc             text eol=lf

# 5. EXCEÇÕES WINDOWS (Mantém CRLF nativo)
# Apenas arquivos batch do Windows precisam de CRLF
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.ps1     text eol=lf  # PowerShell Core (Linux/Docker) prefere LF

# 6. ARQUIVOS BINÁRIOS (Git não deve tocar)
# Imagens
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.ico     binary
*.svg     binary
*.webp    binary

# Fontes
*.ttf     binary
*.eot     binary
*.otf     binary
*.woff    binary
*.woff2   binary

# Arquivos compactados e documentos
*.zip     binary
*.tar.gz  binary
*.tgz     binary
*.rar     binary
*.7z      binary
*.pdf     binary
*.doc     binary
*.docx    binary
*.xls     binary
*.xlsx    binary
*.ppt     binary
*.pptx    binary

# Node
package-lock.json binary
yarn.lock         binary
