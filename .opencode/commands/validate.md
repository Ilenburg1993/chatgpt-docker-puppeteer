---
description: Rodar validação completa (lint/format/test) e sugerir correções
---

Rode as validações padrão do projeto e analise o resultado:

!`npm run validate:all`

Se houver falhas:

- aponte a causa raiz (arquivo + regra/erro)
- proponha um patch mínimo e consistente com o estilo do repo
- sugira o comando exato para revalidar
